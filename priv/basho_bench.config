{mode, max}.
%{mode, {rate, 1000}}.

{duration, 15}.

{concurrent, 1}.

{driver, basho_bench_driver_judy}.

{code_paths, ["../judy/ebin"]}.

{operations, [{mget,1}]}.

{key_generator, {uniform_int, 10000000}}.

{value_generator, {fixed_bin, 1}}.


{initial_keys, 10000000}.
{mget_keys, 1000}.